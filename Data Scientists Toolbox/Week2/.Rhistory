}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
print(length(isComplete)
print(threshold)
if (length(isComplete) > threshold)
{
myResult <- append(myResult, cor(pollutantFile[["sulfate"]], pollutantFile[["nitrate"]]))
}
print(myResult)
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
print(length(isComplete)
print(threshold)
if (length(isComplete) > threshold)
{
myResult <- append(myResult, cor(pollutantFile[["sulfate"]], pollutantFile[["nitrate"]]))
}
##print(myResult)
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
print(length(isComplete)
print(threshold)
if (length(isComplete) > threshold)
{
myResult <- append(myResult, cor(pollutantFile[["sulfate"]], pollutantFile[["nitrate"]]))
}
##print(myResult)
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
print(length(isComplete))
print(threshold)
if (length(isComplete) > threshold)
{
myResult <- append(myResult, cor(pollutantFile[["sulfate"]], pollutantFile[["nitrate"]]))
}
##print(myResult)
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
print(length(isComplete[isComplete == TRUE]))
if (length(isComplete[isComplete == TRUE]) > threshold)
{
myResult <- append(myResult, cor(pollutantFile[["sulfate"]], pollutantFile[["nitrate"]]))
}
##print(myResult)
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
if (length(isComplete[isComplete == TRUE]) > threshold)
{
print(length(isComplete[isComplete == TRUE]))
myResult <- append(myResult, cor(pollutantFile[["sulfate"]], pollutantFile[["nitrate"]]))
}
##print(myResult)
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
if (length(isComplete[isComplete == TRUE]) > threshold)
{
myResult <- append(myResult, cor(pollutantFile[["sulfate"]], pollutantFile[["nitrate"]]))
}
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
head(cr)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
if (length(isComplete[isComplete == TRUE]) > threshold)
{
myResult <- append(myResult, cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"]))
}
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
head(cr)
cr <- corr("specdata", 150)
head(cr)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
if (length(isComplete[isComplete == TRUE]) > threshold)
{
print(cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"]))
##myResult <- append(myResult, cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"]))
}
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
if (length(isComplete[isComplete == TRUE]) > threshold)
{
print(cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"]))
##myResult <- append(myResult, cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"]))
}
else
{
print(paste(id, " else", sep = "")
}
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
if (length(isComplete[isComplete == TRUE]) > threshold)
{
print("if")
##myResult <- append(myResult, cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"]))
}
else
{
print(paste(id, " else", sep = "")
}
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
if (length(isComplete[isComplete == TRUE]) > threshold)
{
print("if")
##myResult <- append(myResult, cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"]))
}
else
{
print(paste(id, " else", sep = "")
}
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
if (length(isComplete[isComplete == TRUE]) > threshold)
{
print("if")
##myResult <- append(myResult, cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"]))
}
else
{
print(paste(id, " else", sep = "")
}
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
if (length(isComplete[isComplete == TRUE]) > threshold)
{
print("if")
##myResult <- append(myResult, cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"]))
}
else
{
print(paste(id, " else", sep = ""))
}
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
if (length(isComplete[isComplete == TRUE]) > threshold)
{
print(cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"]))
##myResult <- append(myResult, cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"]))
}
else
{
print(paste(i, " else", sep = ""))
}
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
if (length(isComplete[isComplete == TRUE]) > threshold)
{
print(cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"], use = "complete.obs"))
##myResult <- append(myResult, cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"]))
}
else
{
print(paste(i, " else", sep = ""))
}
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
if (length(isComplete[isComplete == TRUE]) > threshold)
{
##myResult <- append(myResult, cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"], use = "complete.obs"))
}
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
cr <- corr("specdata", 150)
head(cr)
corr <- function(directory, threshold = 0)
{
myResult <- vector(mode = "numeric", length = 0)
for (i in (dir(directory)))
{
filename <- paste('./', directory, '/', i, sep = "")
pollutantFile <- read.csv(filename)
isComplete <- complete.cases(pollutantFile)
if (length(isComplete[isComplete == TRUE]) > threshold)
{
myResult <- append(myResult, cor(pollutantFile[, "sulfate"], pollutantFile[, "nitrate"], use = "complete.obs"))
}
}
myResult
}
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
## NOTE: Do not round the result!
head(cr)
cr <- corr("specdata", 150)
head(cr)
summary(cr)
cr <- corr("specdata", 400)
head(cr)
summary(cr)
cr <- corr("specdata", 5000)
summary(cr)
length(cr)
cr <- corr("specdata")
summary(cr)
length(cr)
submit()
submit()
submit()
